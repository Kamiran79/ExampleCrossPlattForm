using System;
using System.Collections.Generic;

using Xamarin.Forms;

namespace ExampleCrossPlattForm.Views
{
    public partial class SliderEx : ContentPage
    {
        public SliderEx()
        {
            InitializeComponent();
        }

        //void sli_ValueChanged(System.Object sender, Xamarin.Forms.ValueChangedEventArgs e)
        //{
            //lbl.Text = String.Format("Value is");
            //lbl.Text = e.NewValue.ToString();
          //  lbl.Text = String.Format("Value of Slider is {0:F2}", e.NewValue);
            //double size = double.Parse(e.NewValue.ToString());
            //size = Int32.Parse(e.NewValue.ToString());
           // lbl.FontSize = size;


        //}
    }
}
